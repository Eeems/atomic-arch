#########################################
# THIS FILE IS DYNAMICALLY GENERATED    #
# Run the following to update the file: #
#   $ ./make.py workflow                #
#########################################
name: Build images
on:
  pull_request: &on-filter
    branches:
      - master
    paths:
      - .github/workflows/build.yaml
      - .github/workflows/build-variant.yaml
      - .github/workflows/delta.yaml
      - .github/workflows/iso.yaml
      - .github/workflows/manifest.yaml
      - .github/workflows/sync.yaml
      - ".github/actions/**"
      - make.py
      - seccomp.json
      - .containerignore
      - "overlay/**"
      - "templates/**"
      - "variants/**"
  push: *on-filter
  workflow_dispatch:
  schedule:
    - cron: "0 23 * * *"

concurrency:
  group: build-${{ github.ref_name }}
  cancel-in-progress: true

jobs:

  ######################################
  #             UNIQUE             #
  ######################################

  notifications:
    name: Clear notifications
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4
      - name: Clean cancellation notifications
        uses: ./.github/actions/clean-cancelled-notifications
        with:
          pat: ${{ secrets.NOTIFICATION_PAT }}

  check:
    name: Ensure config is valid
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
    container:
      image: ghcr.io/eeems/atomic-arch-builder:latest
      options: >-
        --privileged
        --security-opt seccomp=unconfined
        --security-opt apparmor=unconfined
        --cap-add=SYS_ADMIN
        --cap-add=NET_ADMIN
        --device /dev/fuse
        --tmpfs /tmp
        --tmpfs /run
        --userns=host
        -v /var/lib/containers:/var/lib/containers
        -v /:/run/host
    steps:
      - name: Checkout the repository
        uses: actions/checkout@v4
      - name: Cache venv
        uses: actions/cache@v4
        with:
          path: .venv
          key: venv-${{ hashFiles('.github/workflows/Dockerfile.builder') }}-${{ hashFiles('make.py') }}
      - name: Ensure config is valid
        run: |
          set -e
          ./make.py check
          ./make.py workflow
          if [[ -n "$(git status -s)" ]]; then
            echo "Please run ./make.py workflow, commit the changes, and try again."
            exit 1
          fi
        env:
          TMPDIR: ${{ runner.temp }}

  manifest:
    name: Generate manifest
    needs:
      - delta_atomic
      - delta_atomic-nvidia
      - delta_atomic-slim
      - delta_base
      - delta_base-nvidia
      - delta_base-slim
      - delta_eeems
      - delta_eeems-nvidia
      - delta_eeems-slim
      - delta_eeems-system76
      - delta_gnome
      - delta_gnome-nvidia
      - delta_gnome-slim
      - delta_rootfs
    uses: ./.github/workflows/manifest.yaml
    secrets: inherit
    permissions: &permissions
      contents: write
      actions: write
      packages: write
      security-events: write
    with:
      cache: false

  sync:
    name: Sync repositories
    needs: manifest
    uses: ./.github/workflows/sync.yaml
    secrets: inherit
    permissions: *permissions

  ######################################
  #             BUILD              #
  ######################################

  rootfs:
    name: Build atomic-arch:rootfs image
    needs: check
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: rootfs
      push: true

  base:
    name: Build atomic-arch:base image
    needs: rootfs
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base
      updates: ${{ fromJson(needs['rootfs'].outputs.updates) }}
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['rootfs'].outputs.updates) && 'rootfs' || '' }}
      digest: ${{ needs['rootfs'].outputs.digest }}

  gnome:
    name: Build atomic-arch:gnome image
    needs: base
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome
      updates: ${{ fromJson(needs['base'].outputs.updates) }}
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['base'].outputs.updates) && 'base' || '' }}
      digest: ${{ needs['base'].outputs.digest }}

  atomic:
    name: Build atomic-arch:atomic image
    needs: base
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic
      updates: ${{ fromJson(needs['base'].outputs.updates) }}
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['base'].outputs.updates) && 'base' || '' }}
      digest: ${{ needs['base'].outputs.digest }}

  base-slim:
    name: Build atomic-arch:base-slim image
    needs: base
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base-slim
      updates: ${{ fromJson(needs['base'].outputs.updates) }}
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['base'].outputs.updates) && 'base' || '' }}
      digest: ${{ needs['base'].outputs.digest }}

  base-nvidia:
    name: Build atomic-arch:base-nvidia image
    needs: base
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base-nvidia
      updates: ${{ fromJson(needs['base'].outputs.updates) }}
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['base'].outputs.updates) && 'base' || '' }}
      digest: ${{ needs['base'].outputs.digest }}

  gnome-slim:
    name: Build atomic-arch:gnome-slim image
    needs: gnome
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome-slim
      updates: ${{ fromJson(needs['gnome'].outputs.updates) }}
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['gnome'].outputs.updates) && 'gnome' || '' }}
      digest: ${{ needs['gnome'].outputs.digest }}

  gnome-nvidia:
    name: Build atomic-arch:gnome-nvidia image
    needs: gnome
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome-nvidia
      updates: ${{ fromJson(needs['gnome'].outputs.updates) }}
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['gnome'].outputs.updates) && 'gnome' || '' }}
      digest: ${{ needs['gnome'].outputs.digest }}

  atomic-slim:
    name: Build atomic-arch:atomic-slim image
    needs: atomic
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic-slim
      updates: ${{ fromJson(needs['atomic'].outputs.updates) }}
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['atomic'].outputs.updates) && 'atomic' || '' }}
      digest: ${{ needs['atomic'].outputs.digest }}

  atomic-nvidia:
    name: Build atomic-arch:atomic-nvidia image
    needs: atomic
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic-nvidia
      updates: ${{ fromJson(needs['atomic'].outputs.updates) }}
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['atomic'].outputs.updates) && 'atomic' || '' }}
      digest: ${{ needs['atomic'].outputs.digest }}

  eeems:
    name: Build atomic-arch:eeems image
    needs: atomic
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems
      updates: ${{ fromJson(needs['atomic'].outputs.updates) }}
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['atomic'].outputs.updates) && 'atomic' || '' }}
      digest: ${{ needs['atomic'].outputs.digest }}

  eeems-slim:
    name: Build atomic-arch:eeems-slim image
    needs: eeems
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-slim
      updates: ${{ fromJson(needs['eeems'].outputs.updates) }}
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['eeems'].outputs.updates) && 'eeems' || '' }}
      digest: ${{ needs['eeems'].outputs.digest }}

  eeems-nvidia:
    name: Build atomic-arch:eeems-nvidia image
    needs: eeems
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-nvidia
      updates: ${{ fromJson(needs['eeems'].outputs.updates) }}
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['eeems'].outputs.updates) && 'eeems' || '' }}
      digest: ${{ needs['eeems'].outputs.digest }}

  eeems-system76:
    name: Build atomic-arch:eeems-system76 image
    needs: eeems
    uses: ./.github/workflows/build-variant.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-system76
      updates: ${{ fromJson(needs['eeems'].outputs.updates) }}
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['eeems'].outputs.updates) && 'eeems' || '' }}
      digest: ${{ needs['eeems'].outputs.digest }}

  ######################################
  #             SCAN               #
  ######################################

  scan_rootfs:
    name: Scan image for rootfs
    if: github.event_name != 'pull_request' || fromJson(needs['rootfs'].outputs.updates)
    needs: rootfs
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: rootfs
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['rootfs'].outputs.updates) && 'rootfs' || '' }}
      digest: ${{ needs['rootfs'].outputs.digest }}

  scan_base:
    name: Scan image for base
    if: github.event_name != 'pull_request' || fromJson(needs['base'].outputs.updates)
    needs: base
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['base'].outputs.updates) && 'base' || '' }}
      digest: ${{ needs['base'].outputs.digest }}

  scan_gnome:
    name: Scan image for gnome
    if: github.event_name != 'pull_request' || fromJson(needs['gnome'].outputs.updates)
    needs: gnome
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['gnome'].outputs.updates) && 'gnome' || '' }}
      digest: ${{ needs['gnome'].outputs.digest }}

  scan_atomic:
    name: Scan image for atomic
    if: github.event_name != 'pull_request' || fromJson(needs['atomic'].outputs.updates)
    needs: atomic
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['atomic'].outputs.updates) && 'atomic' || '' }}
      digest: ${{ needs['atomic'].outputs.digest }}

  scan_base-slim:
    name: Scan image for base-slim
    if: github.event_name != 'pull_request' || fromJson(needs['base-slim'].outputs.updates)
    needs: base-slim
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base-slim
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['base-slim'].outputs.updates) && 'base-slim' || '' }}
      digest: ${{ needs['base-slim'].outputs.digest }}

  scan_base-nvidia:
    name: Scan image for base-nvidia
    if: github.event_name != 'pull_request' || fromJson(needs['base-nvidia'].outputs.updates)
    needs: base-nvidia
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base-nvidia
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['base-nvidia'].outputs.updates) && 'base-nvidia' || '' }}
      digest: ${{ needs['base-nvidia'].outputs.digest }}

  scan_gnome-slim:
    name: Scan image for gnome-slim
    if: github.event_name != 'pull_request' || fromJson(needs['gnome-slim'].outputs.updates)
    needs: gnome-slim
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome-slim
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['gnome-slim'].outputs.updates) && 'gnome-slim' || '' }}
      digest: ${{ needs['gnome-slim'].outputs.digest }}

  scan_gnome-nvidia:
    name: Scan image for gnome-nvidia
    if: github.event_name != 'pull_request' || fromJson(needs['gnome-nvidia'].outputs.updates)
    needs: gnome-nvidia
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome-nvidia
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['gnome-nvidia'].outputs.updates) && 'gnome-nvidia' || '' }}
      digest: ${{ needs['gnome-nvidia'].outputs.digest }}

  scan_atomic-slim:
    name: Scan image for atomic-slim
    if: github.event_name != 'pull_request' || fromJson(needs['atomic-slim'].outputs.updates)
    needs: atomic-slim
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic-slim
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['atomic-slim'].outputs.updates) && 'atomic-slim' || '' }}
      digest: ${{ needs['atomic-slim'].outputs.digest }}

  scan_atomic-nvidia:
    name: Scan image for atomic-nvidia
    if: github.event_name != 'pull_request' || fromJson(needs['atomic-nvidia'].outputs.updates)
    needs: atomic-nvidia
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic-nvidia
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['atomic-nvidia'].outputs.updates) && 'atomic-nvidia' || '' }}
      digest: ${{ needs['atomic-nvidia'].outputs.digest }}

  scan_eeems:
    name: Scan image for eeems
    if: github.event_name != 'pull_request' || fromJson(needs['eeems'].outputs.updates)
    needs: eeems
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['eeems'].outputs.updates) && 'eeems' || '' }}
      digest: ${{ needs['eeems'].outputs.digest }}

  scan_eeems-slim:
    name: Scan image for eeems-slim
    if: github.event_name != 'pull_request' || fromJson(needs['eeems-slim'].outputs.updates)
    needs: eeems-slim
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-slim
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['eeems-slim'].outputs.updates) && 'eeems-slim' || '' }}
      digest: ${{ needs['eeems-slim'].outputs.digest }}

  scan_eeems-nvidia:
    name: Scan image for eeems-nvidia
    if: github.event_name != 'pull_request' || fromJson(needs['eeems-nvidia'].outputs.updates)
    needs: eeems-nvidia
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-nvidia
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['eeems-nvidia'].outputs.updates) && 'eeems-nvidia' || '' }}
      digest: ${{ needs['eeems-nvidia'].outputs.digest }}

  scan_eeems-system76:
    name: Scan image for eeems-system76
    if: github.event_name != 'pull_request' || fromJson(needs['eeems-system76'].outputs.updates)
    needs: eeems-system76
    uses: ./.github/workflows/scan.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-system76
      push: ${{ github.event_name != 'pull_request' }}
      artifact: ${{ fromJson(needs['eeems-system76'].outputs.updates) && 'eeems-system76' || '' }}
      digest: ${{ needs['eeems-system76'].outputs.digest }}

  ######################################
  #             DELTA              #
  ######################################

  delta_rootfs:
    name: Generate deltas for rootfs
    if: github.event_name != 'pull_request'
    needs: rootfs
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: rootfs
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  delta_base:
    name: Generate deltas for base
    if: github.event_name != 'pull_request'
    needs: base
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  delta_gnome:
    name: Generate deltas for gnome
    if: github.event_name != 'pull_request'
    needs: gnome
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  delta_atomic:
    name: Generate deltas for atomic
    if: github.event_name != 'pull_request'
    needs: atomic
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  delta_base-slim:
    name: Generate deltas for base-slim
    if: github.event_name != 'pull_request'
    needs: base-slim
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base-slim
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  delta_base-nvidia:
    name: Generate deltas for base-nvidia
    if: github.event_name != 'pull_request'
    needs: base-nvidia
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base-nvidia
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  delta_gnome-slim:
    name: Generate deltas for gnome-slim
    if: github.event_name != 'pull_request'
    needs: gnome-slim
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome-slim
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  delta_gnome-nvidia:
    name: Generate deltas for gnome-nvidia
    if: github.event_name != 'pull_request'
    needs: gnome-nvidia
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome-nvidia
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  delta_atomic-slim:
    name: Generate deltas for atomic-slim
    if: github.event_name != 'pull_request'
    needs: atomic-slim
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic-slim
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  delta_atomic-nvidia:
    name: Generate deltas for atomic-nvidia
    if: github.event_name != 'pull_request'
    needs: atomic-nvidia
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic-nvidia
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  delta_eeems:
    name: Generate deltas for eeems
    if: github.event_name != 'pull_request'
    needs: eeems
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  delta_eeems-slim:
    name: Generate deltas for eeems-slim
    if: github.event_name != 'pull_request'
    needs: eeems-slim
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-slim
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  delta_eeems-nvidia:
    name: Generate deltas for eeems-nvidia
    if: github.event_name != 'pull_request'
    needs: eeems-nvidia
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-nvidia
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  delta_eeems-system76:
    name: Generate deltas for eeems-system76
    if: github.event_name != 'pull_request'
    needs: eeems-system76
    uses: ./.github/workflows/delta.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-system76
      push: ${{ github.event_name != 'pull_request' }}
      recreate: false

  ######################################
  #             ISO                #
  ######################################

  online_iso_base:
    name: Generate iso for base
    needs: base
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['base'].outputs.updates) }}
      offline: false

  offline_iso_base:
    name: Generate iso for base
    needs: base
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['base'].outputs.updates) }}
      offline: true

  online_iso_gnome:
    name: Generate iso for gnome
    needs: gnome
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['gnome'].outputs.updates) }}
      offline: false

  offline_iso_gnome:
    name: Generate iso for gnome
    needs: gnome
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['gnome'].outputs.updates) }}
      offline: true

  online_iso_atomic:
    name: Generate iso for atomic
    needs: atomic
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['atomic'].outputs.updates) }}
      offline: false

  offline_iso_atomic:
    name: Generate iso for atomic
    needs: atomic
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['atomic'].outputs.updates) }}
      offline: true

  online_iso_base-slim:
    name: Generate iso for base-slim
    needs: base-slim
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base-slim
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['base-slim'].outputs.updates) }}
      offline: false

  offline_iso_base-slim:
    name: Generate iso for base-slim
    needs: base-slim
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base-slim
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['base-slim'].outputs.updates) }}
      offline: true

  online_iso_base-nvidia:
    name: Generate iso for base-nvidia
    needs: base-nvidia
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base-nvidia
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['base-nvidia'].outputs.updates) }}
      offline: false

  offline_iso_base-nvidia:
    name: Generate iso for base-nvidia
    needs: base-nvidia
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: base-nvidia
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['base-nvidia'].outputs.updates) }}
      offline: true

  online_iso_gnome-slim:
    name: Generate iso for gnome-slim
    needs: gnome-slim
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome-slim
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['gnome-slim'].outputs.updates) }}
      offline: false

  offline_iso_gnome-slim:
    name: Generate iso for gnome-slim
    needs: gnome-slim
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome-slim
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['gnome-slim'].outputs.updates) }}
      offline: true

  online_iso_gnome-nvidia:
    name: Generate iso for gnome-nvidia
    needs: gnome-nvidia
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome-nvidia
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['gnome-nvidia'].outputs.updates) }}
      offline: false

  offline_iso_gnome-nvidia:
    name: Generate iso for gnome-nvidia
    needs: gnome-nvidia
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: gnome-nvidia
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['gnome-nvidia'].outputs.updates) }}
      offline: true

  online_iso_atomic-slim:
    name: Generate iso for atomic-slim
    needs: atomic-slim
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic-slim
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['atomic-slim'].outputs.updates) }}
      offline: false

  offline_iso_atomic-slim:
    name: Generate iso for atomic-slim
    needs: atomic-slim
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic-slim
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['atomic-slim'].outputs.updates) }}
      offline: true

  online_iso_atomic-nvidia:
    name: Generate iso for atomic-nvidia
    needs: atomic-nvidia
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic-nvidia
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['atomic-nvidia'].outputs.updates) }}
      offline: false

  offline_iso_atomic-nvidia:
    name: Generate iso for atomic-nvidia
    needs: atomic-nvidia
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: atomic-nvidia
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['atomic-nvidia'].outputs.updates) }}
      offline: true

  online_iso_eeems:
    name: Generate iso for eeems
    needs: eeems
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['eeems'].outputs.updates) }}
      offline: false

  offline_iso_eeems:
    name: Generate iso for eeems
    needs: eeems
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['eeems'].outputs.updates) }}
      offline: true

  online_iso_eeems-slim:
    name: Generate iso for eeems-slim
    needs: eeems-slim
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-slim
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['eeems-slim'].outputs.updates) }}
      offline: false

  offline_iso_eeems-slim:
    name: Generate iso for eeems-slim
    needs: eeems-slim
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-slim
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['eeems-slim'].outputs.updates) }}
      offline: true

  online_iso_eeems-nvidia:
    name: Generate iso for eeems-nvidia
    needs: eeems-nvidia
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-nvidia
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['eeems-nvidia'].outputs.updates) }}
      offline: false

  offline_iso_eeems-nvidia:
    name: Generate iso for eeems-nvidia
    needs: eeems-nvidia
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-nvidia
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['eeems-nvidia'].outputs.updates) }}
      offline: true

  online_iso_eeems-system76:
    name: Generate iso for eeems-system76
    needs: eeems-system76
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-system76
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['eeems-system76'].outputs.updates) }}
      offline: false

  offline_iso_eeems-system76:
    name: Generate iso for eeems-system76
    needs: eeems-system76
    uses: ./.github/workflows/iso.yaml
    secrets: inherit
    permissions: *permissions
    with:
      variant: eeems-system76
      pull: ${{ github.event_name != 'pull_request' && fromJson(needs['eeems-system76'].outputs.updates) }}
      offline: true
